# Website previews — September 3, 2026

Assistant and Stream use complete 1206 × 2622 simulator captures. CSS fits the
entire frame; the image files are not cropped. Assistant was captured from the
installed simulator build; Stream from source build 0.1.6 (20).

For the public Stream capture, the Debug-only `VAMP_SCREENSHOT_PREVIEW=1`
presentation mask replaces discovered Mac names and addresses with generic
labels and shows one row. It does not approve, pair, or stream. The mask is
compiled out of Release builds. No real host identity or address is published.
