import Foundation

enum HostAppLinks {
    static let websiteURL = URL(string: "https://thevamp.app/")!
    static let websiteDisplayPath = "thevamp.app"
    /// Public GitHub clone URL. The repository is still named macpair; do not
    /// point this at Mesutcydev/vamp until that rename exists with redirects.
    static let vampTerminalURL = URL(string: "https://github.com/Mesutcydev/macpair")!
    static let vampTerminalDisplayPath = "github.com/Mesutcydev/macpair"
}
